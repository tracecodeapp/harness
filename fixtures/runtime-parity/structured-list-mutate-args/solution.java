import java.util.*;

class Solution {
  public List<List<Integer>> solve() {
    int r = 0;
    int c = 1;
    List<List<Integer>> cells = new ArrayList<>();
    cells.add(Arrays.asList(r, c));
    return cells;
  }
}
