/**
 * Legacy source-level barrel for the public execution and verdict contracts.
 *
 * New code should import the semantic module it needs. The package root
 * continues to expose the same named contracts.
 */
export * from './runtime-execution';
export * from './judge-contracts';
